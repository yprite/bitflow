'use client';

import TidalGrid from './TidalGrid';

/**
 * Client component wrapper for the ambient background.
 * Placed in layout.tsx to provide the living dot field behind all content.
 */
export default function AmbientBackground() {
  return <TidalGrid />;
}
